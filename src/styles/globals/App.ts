import { styled } from '../helpers/styled';

export const AppStyles = styled.createGlobalStyle`
    #root {
        width: 100%;
        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }
    }
`;
