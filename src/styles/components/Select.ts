import { styled } from '../helpers/styled';

export const Select = styled.select`
    padding: 8px;
    border-radius: 4px;
    min-width: 120px;

    @media (max-width: 768px) {
        width: 100%;
    }
`;
